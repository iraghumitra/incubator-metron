import { AlertDetailsKeysPipe } from './alert-details-keys.pipe';

describe('AlertDetailsKeysPipe', () => {
  it('create an instance', () => {
    const pipe = new AlertDetailsKeysPipe();
    expect(pipe).toBeTruthy();
  });
});
